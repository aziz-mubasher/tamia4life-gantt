services:
  - type: web
    name: azm-lean-startup-roadmap
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: SUPABASE_URL
        value: https://euuqijuyuoqgzclhmhki.supabase.co
      # Set in Render dashboard → Environment (secret):
      # SUPABASE_SERVICE_ROLE_KEY = service_role key from Supabase API settings
