services:
  - type: web
    name: azm-lean-startup-roadmap
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
