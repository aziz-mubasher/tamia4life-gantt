services:
  - type: web
    name: azm-lean-startup-roadmap
    runtime: node
    plan: free
    buildCommand: echo "Web app has no npm dependencies"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
