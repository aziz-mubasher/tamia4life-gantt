services:
  - type: web
    name: azm-lean-startup-roadmap
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      # Add in Render dashboard (Settings → Environment):
      # SUPABASE_URL=https://euuqijuyuoqgzclhmhki.supabase.co
      # SUPABASE_SERVICE_ROLE_KEY=<service_role key from Supabase API settings>
